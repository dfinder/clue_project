#This should have two options, to be the host, or to be a client
#The client option should ask for the address and port of the host, then on a successful connection, drop you into piece selection.
#On an unsuccessful connection, it should pop up an error box
#The host option should shove you immediately into piece piece selection.