#Everything should generally be forwarded to the notes controller, even by default! 
#We can probably make an annotation to this end. 
#Every message should have a human readable message, so we can debug inside the game itself!
